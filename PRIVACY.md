# Privacy — HE Keyword Steroids

No data leaves your machine except Google Suggest / Search requests to `suggestqueries.google.com` and `google.com/search` with `hl=iw gl=il`. No account, no key, no tracking. Reports are written locally to `--out` only. No cookies, no analytics in the tool.

If you run the hosted tier (future), the same rule applies — queries are not stored, reports are yours.

Contact: via Gumroad listing.
